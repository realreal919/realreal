import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "註冊",
  description: "建立誠真生活 RealReal 帳號，享受純素健康食品購物體驗。",
}

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
